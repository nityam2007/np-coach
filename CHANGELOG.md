# CHANGELOG

Append-only. Newest entries at the bottom. Never edit or delete past entries — only add.

---

## 2026-07-09

- **Fixed dev-server `ChunkLoadError` on `/` and `/account/login`** — corrupted Turbopack cache (`SyntaxError: Invalid regular expression: missing /` inside Next's own emitted chunk, not source). Cleared `.next` (733M stale). No source changes. See [RULES.md](RULES.md) → Common errors.
- **Verified homepage data path** — `settings.homepage.*`, `faqs`, `stats`, `coverage`, `school` all present in Directus; typecheck + lint clean. No changes needed.
- **Confirmed seed already applied** — settings(1), services(4), fleet(6), testimonials(3), stops(6). No re-seed run.
- **Added `RULES.md` and `CHANGELOG.md`** — operating rules (AI don'ts, common errors + fixes) and this append-only history.
- **Dummy checkout for ticket booking + lost property** — when `STRIPE_SECRET_KEY` is unset, `startBooking`/`startPassPurchase` now persist the row, mark it `paid` via new `markPaidByReference()`, and redirect straight to the success/ticket page (Stripe fully bypassed). With Stripe keys set, behaviour is unchanged (real Checkout). Marked `ponytail:` in `stripe.ts` + `actions.ts` — remove both branches once live keys land.
- **Frontend: always render the booking + lost-property forms** — dropped the `stripeConfigured()` gate on `daily-express-service/book` and `lost-property/claim` pages that hid the form behind a "payments being set up / call us" notice. The forms now show and drive the dummy checkout above. Removed the now-unused `stripeConfigured` imports.
- **Homepage UI polish — depth + motion, pure-CSS (no runtime dep)** — added reusable keyframe primitives to `globals.css` (`.border-beam` traveling light via conic-gradient + `@property`, `.shiny-text` shimmer, `.reveal` scroll-in via `animation-timeline: view()`, `.animate-float`), all gated behind `prefers-reduced-motion`. New tiny `ui/BorderBeam.tsx` (MagicUI Border Beam ported **without** the `motion` dependency). Applied across: `HeroSearch` (border beam + gradient/animated Search button, rotating swap, icon lift, carded trust features), `Hero` (gradient CTAs, hero-photo hover-zoom + gradient overlay + floating "15+ Euro-6" trust badge + live status dot + shiny eyebrow + floating decorative ring), `FleetCarousel` (border-beam frame, 5s auto-advance that pauses on hover/focus, image zoom + rise-in, spec-row + arrow hover), `SchoolTransportBlock` + `StatsBand` (navy gradient + grid/glow texture, hover states, stat cards rise-in on scroll), `CoverageMap` (animated SVG — hub ping ring, dash-flow route lines, pulsing nodes, floating map, interactive pills), `AccreditationStrip` (grayscale→colour logos on hover, lifted card). `Eyebrow` gained an opt-in `shiny` prop. No new dependency; typecheck + lint clean.
- **Maximalist motion pass — homepage + header + footer (adds `motion` / framer-motion)** — installed `motion` (v12) and added reusable primitives in `ui/motion.tsx` (`Reveal`, `Stagger`/`StaggerItem`, `Parallax`, `Tilt` 3D cursor-tilt, `Magnetic` cursor-pull) and animated decorative backdrops in `ui/Backdrops.tsx` (`StripesBackdrop` drifting diagonal stripes, `FloatingBlobs`, `GridBackdrop`). All respect `prefers-reduced-motion` via `useReducedMotion`.
  - **Header** — redesigned: navy announcement top-bar (since 1999 · Euro-6 · phone), scroll-shrink/solidify header, sliding active-nav pill (`layoutId`), animated hamburger→X, right-side spring **mobile drawer** with scrim + body-scroll-lock + staggered links + animated sub-menus, magnetic Get-a-Quote CTA, phone in an accent chip.
  - **Footer** — now client + animated: drifting navy stripes + blobs, a closing CTA band, reveal-on-scroll staggered columns, icon contact block, animated underline on links, magnetic CTA.
  - **Hero** — animated blob backdrop, staggered copy entrance, **3D tilt** on the coach photo with a parallaxed floating trust badge, magnetic primary CTA.
  - **Sections** — `ServiceCards` (staggered card reveal + shiny eyebrow), `FleetCarousel` (reveal + tilt on the frame), `AccreditationStrip` (reveal), school/coverage/stats/CTA blocks wrapped in `Reveal` + subtle `Tilt`; `TestimonialCarousel` quote now cross-fades/slides via `AnimatePresence`; `FaqAccordion` animates height on expand + polished toggle. Closing CTA band got the animated stripe/blob backdrop.
  - Verified: `tsc --noEmit` + `npm run lint` clean; all `motion/react` APIs resolve at the installed version.
- **Header + footer trim per feedback** — Header: removed the navy announcement top-bar ("Family-run since 1999 · … · phone") and the desktop phone/contact chip entirely; desktop header is now **Logo · Menu · Account · Get-a-Quote**. Account stays in the mobile drawer (with tap-to-call phone + Get a Quote). Smoothed the scroll-shrink (higher-contrast easing, gentler padding/logo-height easing on `transition-[height]`) so it no longer jumps. Footer: removed the first column (NP logo + brand blurb + phone); grid is now 4 columns (`lg:grid-cols-4`) — the CMS `footerColumns` + Contact. tsc + lint clean.
- **Footer trim + homepage CTA removal + hero rounding fix** — Footer now omits the "School Transport" column (removed from `site-content.json` footerColumns + a defensive `.filter()` in `Footer.tsx`), leaving **Services · Daily Express · Company · Contact** (4 cols). Removed the homepage closing "Ready to Book Your Journey?" search-CTA section from `page.tsx` (kept the footer's own compact CTA band); dropped the now-unused `HeroSearch`/`Eyebrow`/backdrop imports. **Hero image rounding bug fixed** — the coach photo's rounded corners were being clipped to square mid-`Tilt`/hover-scale (browser dropping the parent's border-radius on a composited scaling child). Fixed with `transform-gpu` + `[isolation:isolate]` + `[will-change:transform]` on the clip container and a fallback `rounded-3xl` on the `<Image>` itself.
- **CMS overhaul (`npm run configure`) — admin UX + Insights + abandoned carts** — applied live to the running Directus. Added an **Accounts** group (customers, otp_codes). Every collection got a **plain-English note** for a non-technical admin ("what is this / what do I do here"). Rebuilt the **NP Coaches** dashboard to **15 panels**: revenue today/7d/30d/all-time, paid bookings by window, pass revenue, **🛒 abandoned carts** (bookings/passes still `pending` >30 min after `created_at` — with the £ value left on the table) computed via `$NOW(-30 minutes)` filters, weekly leads, and recent-activity lists (paid bookings, abandoned carts to follow up, messages/quotes). Business facts (address/phone/legal name/fleet) were already correct in the seed. Verified panel filters persisted correctly.
- **Hero image rounding — proper fix** — the earlier transform-gpu/isolate patch didn't hold: the real cause was the `<Image>` scaling on hover *inside* a rounded `overflow-hidden` clip that sits under `Tilt`'s 3D transform — the separately-composited scaled image bled past the static rounded clip, squaring the corners. Fix: scale the **rounded container itself** (`group-hover:scale-[1.02]`) instead of the image, so the rounded border scales as one unit and corners can never square off; the image now just `fill`s with no transform of its own.
- **School-transport card fills its height (no dead space)** — the card is `h-full` (matches the CoverageMap beside it) but its content was top-aligned + `items-center`, leaving a large empty band below the checklist/CTA on the left. Reworked the interior (same card size): left column is now `flex flex-col h-full` — header, then the checklist grows (`flex-1 content-center`) to absorb slack, then a **trust footer strip** (Fully insured · DfT compliant · Since 1999) + CTA anchored to the bottom. The photo dropped its fixed `aspect-[4/5]` and stretches to the full column height (`min-h-[22rem]`, `items-stretch`) with a caption overlay filling its bottom band.
- **Shared PageHero + SEO helpers → interior pages match the homepage** — added three reusables and applied them:
  - `src/components/sections/PageHero.tsx` — the polished animated header (navy gradient + stripes/blobs backdrop + breadcrumb + shiny eyebrow + reveal + optional CTAs). Replaces the ~10 hand-rolled `<section className="bg-navy text-offwhite">` headers.
  - `src/lib/seo.ts` — `buildMetadata({title,description,path,titleAbsolute,noIndex})`: one call sets title/description + **canonical** + per-page OpenGraph/Twitter, consistently.
  - `src/components/seo/JsonLd.tsx` — `BreadcrumbJsonLd`, `ItemListJsonLd`, `ContactPageJsonLd`, `FaqJsonLd`.
  - Applied to **fleet, uk-tours, blog, contact-us, get-a-quote, daily-express-service, home-to-school** (PageHero + buildMetadata + Breadcrumb/ItemList/ContactPage JSON-LD) and to **PageTemplate** (upgrades every CMS content page at once). Canonicals now cover the static pages that lacked them; breadcrumbs added sitewide on those routes. (Detail pages with bespoke image/back-link headers — vehicle, route, blog post, school, claim, book — kept their layouts; they already had metadata + canonical + type JSON-LD.)
  - Confirmed `FaqAccordion` already emits FAQPage JSON-LD internally (home-to-school + homepage FAQs are covered — no duplicate added). OG image skipped: no brand asset yet (drop `public/og.jpg` + one layout line when the logo lands).
- **Lost-property email configured (Outlook SMTP)** — `.env.example` now sets `SMTP_HOST=smtp-mail.outlook.com`, `SMTP_PORT=587`, `SMTP_SECURE=false` (STARTTLS), `SMTP_USER`/`SMTP_FROM=npclostproperty@outlook.com` (password left blank for the client to fill), and a new `LOST_PROPERTY_TO=info@np-coaches.co.uk`. `startPassPurchase` now fires a claim-notification email to that address (fire-and-forget; no-ops gracefully until the SMTP password is set). The existing env-driven `sendEmail` (`src/lib/email.ts`) needed no code change.
- **Detail pages redesigned to match the homepage (awwwards pass)** — the `[slug]` **vehicle** page (e.g. `/19-seat-coaches`) and **route** page, plus `uk-tours/[destination]`, `blog/[slug]`, and `home-to-school/[school]`, were still using flat `bg-navy` headers + plain bordered lists. Rebuilt with the shared motion system: animated navy gradient hero (stripes + blobs backdrop, breadcrumb, shiny eyebrow, reveal), a **tilted photo** frame (corners-safe zoom), **icon-badged spec cards** (staggered) + hover-zoom gallery on the vehicle page, `arrow`-styled route title, reveal-in prose bodies, magnetic gradient CTAs, and closing CTA bands. Added `BreadcrumbJsonLd` to every detail page. Extracted the fleet `featureIcon` map to `src/lib/feature-icon.ts` (shared by the carousel + vehicle page). The two transactional form pages (`book`, `lost-property/claim`) got the gradient + stripe backdrop on their headers only (kept light — the form stays the focus). No flat `bg-navy text-offwhite` headers remain sitewide. tsc + lint clean.
- **Missing sitemap pages backfilled (8) + seed made per-slug idempotent** — cross-checked `AIDATA/SiteMAP.md` against the live CMS and found only **2 of 10** content pages were actually seeded into Directus (`about-us`, `contact-us`); FAQs, Safeguarding, Vacancies, Downloads, Privacy Policy, Cookie Policy, Terms and Lost Property were **missing** (they'd 404 / fall back). Root cause: `scripts/seed-directus.mjs` seeded pages all-or-nothing (`pages?limit=1` → skip if any exist). Fixed to insert **per-slug** (fetch existing slugs, insert only missing — never clobbers client edits), then re-ran `npm run seed` → backfilled all 8. They render through `[slug]` → `PageTemplate`, which already has the animated PageHero, so they're live **and** styled. Verified 10 pages now in Directus. Transactional/receipt pages (book, claim, booking/success, pass/success, account, ticket) intentionally kept as clean utility screens (no heavy hero). Every sitemap entry now maps to a working, styled route. tsc + lint clean.
- **OTP rows now purged (were flagged-but-never-deleted)** — used/expired OTP codes were correctly invalidated (`consumed: true`) so they can't be replayed, but the rows lived in `otp_codes` forever (unbounded growth + lingering email PII). Added `sweepOldOtps()` in `src/lib/account.ts` — an opportunistic batch DELETE of rows whose `expires_at` is >24h in the past, called at the top of `createAndSendOtp` (the collection only grows on login requests, so cleaning there keeps it self-bounded; no cron needed). Security logic unchanged. Verified live against Directus: the `expires_at < cutoff` filter matched a planted stale row and the batch `DELETE /items/otp_codes` (204) removed it while a fresh row survived.


## 2026-07-24

- **Enabled the NSheth review host** — production Traefik now routes `demo3000.nsheth.in` to the same Next.js app as `SITE_HOST`, and Directus permits that exact HTTPS origin via its explicit CORS array. `DEMO_HOST` is configurable in `.env.prod` (defaulted in `.env.example`) without changing the canonical public site URL.


- **Enabled the NSheth Directus review host** — Traefik also routes https://demo8055.nsheth.in/admin to the existing Directus service through configurable DEMO_DIRECTUS_HOST, with no duplicate CMS or widened CORS policy.

- **Homepage UI made calmer & more professional (CRO pass)** — removed the "quirky" ambient animation layer sitewide while keeping the proven section hierarchy (hero → accreditations → services → fleet → school/coverage → social proof → FAQ → closing CTA) and all functionality (search widget, carousels, accordion untouched). Deleted `BorderBeam` (component + CSS) from the search bar/fleet card; removed the `shiny-text` eyebrow shimmer (Eyebrow's `shiny` prop dropped, all call sites updated); `Backdrops` (stripes/blobs/grid) are now static textures instead of infinite drifts (no more `motion` runtime there); hero lost its floating blobs, ping dot, floating ring, 3D `Tilt` and `Magnetic` cursor effects (simple 0.5s fade-up entrances instead, still `useReducedMotion`-aware); `Reveal`/`Stagger`/testimonial slide/`rise-in` no longer blur (plain fade/rise); coverage map SVG is static (no dash-flow/pulse `<animate>`s) and no longer floats; gradient CTAs flattened to solid accent with a simple hover darken. Structure: accreditation strip now sits under an "Accredited, licensed & regulated" caption so certifications/regulations read as a deliberate trust section; the footer CTA band now renders the previously-unrendered CMS `homepage.ctaEyebrow/ctaHeading/ctaBody` fields instead of hardcoded copy. Build (all routes prerendered) + lint clean.

- **Booking widget unified into one card** — the hero `HeroSearch` label ("Daily Express — book your tickets"), the From/To/date/Search form row, and the three trust features (Quick & Easy / No Obligation / Secure & Private) were three loose stacked elements; they're now a single white rounded card separated by hairlines (`divide`/`border` at `greyblue/15`): header row → form row → (inline same-stop error) → 3-column feature footer. Feature pills shrunk to compact rows (8×8 accent icon chips, no hover chrome) and the Search button flattened from gradient to solid accent to match the rest of the pass. Form logic, routing, and the `compact` variant untouched.

- **Shadow scale evened out + booking-widget badges muted** — the homepage mixed `shadow-2xl/xl/lg` with heavy tints (up to `navy/30`), making elevation feel uneven. Normalised to one subtle system: standing cards `shadow-sm shadow-navy/5` (accreditation strip, service cards, coverage card, carousel nav), featured containers `shadow-md shadow-navy/5–10` (booking widget, fleet card, school block, stats band), hero photo `shadow-lg shadow-navy/10` as the single strongest anchor, and all accent CTAs `shadow-sm shadow-accent/20 → hover:shadow-md` (header, hero, search, school, footer, mobile menu — last gradient flattened too). Overlays (nav dropdown, mobile drawer) keep stronger elevation by design. The booking widget's trust-feature footer was competing with the form: now a tinted `#fafbfe` band with smaller neutral `navy/5` icon chips and quieter type so the form stays the focal point. Lint clean, page renders.

- **Fleet section rebuilt as a navy showcase band + section background rhythm** — the fleet carousel was a flat white card ("very basic"); it's now a full-bleed navy gradient band (static dotted-grid texture + corner glow) with: white-on-navy copy + white CTA, a frosted `white/5` showcase panel, a **giant ghost seat-numeral** behind the panel (editorial depth, updates per vehicle), the seats badge as an accent pill, spec rows as ringed chips with sky icons, and the anonymous carousel dots replaced by a **seat-count selector rail** (pill per vehicle, e.g. 19/35/53…, `role=tablist`) plus an `01 / 06` counter and dark-styled arrows. Autoplay/pause/keyboard behaviour unchanged; fleet range strip ("19–98 seats · Euro-6 · ULEZ-compliant") computed via min/max, not list order. Page rhythm: homepage was near-uniform white — Services and Testimonials/Stats bands now sit on a soft `#f2f5fb` tint so the flow alternates hero-gradient → offwhite → tint → navy → offwhite → tint → offwhite → navy footer. Lint clean.


## 2026-07-27

- **Fleet photo gallery corrected** — moved the responsive, thumbnail-first lightbox into the existing `FleetDetail` gallery directly after seating layouts, removing the duplicate page-end gallery. Added a documented read-before-edit rule to `RULES.md`, `AGENTS.md`, `CLAUDE.md` and `AIDATA/CONTINUE.md`.
- **Navy replaced with a deep primary-blue family** — the near-black navy `#0e0f27` clashed with the primary blue; the `--color-navy` token is now **`#172554`** (blue-950, the deep shade of accent `#2563eb`) so every `bg-navy/text-navy/shadow-navy` usage recolours sitewide with zero call-site churn. Dark-gradient endpoints `#161838` → `#1e3a8a` (blue-900) across all navy bands (PageHero, fleet showcase, school block, stats band, detail-page heroes, book/claim headers); navy-rgb literals `rgba(14,15,39,…)` → `rgba(23,37,84,…)` (grid backdrop dots, header scroll shadow); `icon.svg` tile and the boarding-pass QR dark colour updated to `#172554` (still ample contrast for scanning). Token *name* `navy` kept deliberately — it's the single source of truth and renaming would churn every component for no visual gain. Lint clean.

- **Services grid → auto-scrolling 3-up carousel** — the four narrow cards were replaced by a snap-scroll track of wider cards (3 visible on desktop via `w-[calc((100%-3rem)/3)]`, 2 on tablet, ~1 on mobile; images now 16:10). Auto-advances one card every 4s and wraps to the start at the end; pauses on hover/focus, disabled under prefers-reduced-motion, native swipe/scroll retained (scrollbar hidden), plus side arrow buttons (sm+). Component became `"use client"` (scroll ref + interval); card markup/copy and the CMS data flow unchanged. Lint clean.

- **Presentation-layer overhaul (design-system pass, Phases 2–5)** — audited then systematized the whole UI with zero logic changes (verified: no server actions, lib logic, API, schema, form `name`/`id`s, routes, or JSON-LD touched; `ticket.ts` diff is only the QR hex from the earlier palette task). **Tokens:** `@theme` gains semantic colours — `brand-hover #1e4fd6`, `brand-deep #1e3a8a`, `tint #e8eefb`, `tint-soft #f2f5fb` — and every raw hex in markup was replaced with them (5 near-dupe light tints collapsed into 2). **A11y:** global `:focus-visible` accent outline (was zero focus styling sitewide); text ramp collapsed from 7 navy opacities to `/70`+`/80` (all AA-passing); form inputs now 16px with a real focus ring. **Scales:** `rounded-md` eliminated (→`lg`); shadows capped at 3 levels (`xl/2xl`→`lg`, `hover:xl`→`md`); section rhythm normalised to `py-16 lg:py-20`. **Primitives:** new `ui/Button.tsx` (primary/secondary/inverse/ghostDark × md/sm) and `ui/field.ts` (`inputCls` etc.) now power all 5 forms (Contact, Booking, LostProperty, Login) and every major CTA — the 3 competing primary-button styles (flat, gradient, `hover:opacity-90`) are now one. **Old design generation retired:** `[slug]`, `uk-tours/[destination]`, `PageHero` lost Tilt/Magnetic/gradient CTAs/heavy shadows (match the homepage language); dead `Tilt`/`Magnetic`/`Parallax` primitives deleted from `ui/motion.tsx`. **New pages:** branded `not-found.tsx` + `error.tsx`. **CRO:** trust rail (Stripe security · e-ticket · phone) on the book page beside the payment form; success pages end with next actions ("Book another journey"); quote-page fallback CTAs on the system. **Copy (flagged):** service cards "Learn more"→"Explore service"; booking success adds "Book another journey"/"View timetable". Docs: new `DESIGN.md` system rules. Build 48/48 static pages, lint clean, all key routes + 404 smoke-tested 200/404.

- **Footer gradient** — flat `bg-navy` footer now uses the same subtle dark-band gradient as every other navy section (`bg-gradient-to-br from-navy via-navy to-brand-deep`), so the page closes with the system's signature deep-blue shift instead of a flat block.

## 2026-07-24

- **CMS, checkout and security hardening** — added live `/get-a-quote` form with protected `quote_requests` storage; added CMS-managed `blog_posts.thumbnail` previews and applied the schema locally with `npm run seed`.
- Removed anonymous Directus form writes, added bounded hashed-identity rate limiting to contact, quote, checkout and OTP flows, and added Turnstile to OTP requests.
- Removed the no-Stripe paid bypass. Checkout success pages verify the Stripe session server-side, show no ticket or paid claim until verification succeeds, and restrict webhooks to NP Coaches' explicit payment kinds. Stripe receives the customer email for receipts.
- Ticket PNG/PDF export now uses high-resolution Blob capture and a correctly scaled A4 PDF.

- **Fleet and interior-page rebuild from the WordPress archive** — `/fleet` and all six coach detail pages now use the archived structure: CMS-managed overview copy, luxury/group introduction, facilities, seating plans, quick-preview galleries, premium charter CTA and four booking steps. Added `settings.fleet_page`, `fleet.group_label`, `fleet.layout_images` and `pages.image`; linked the original per-vehicle gallery/layout files without overwriting editor media. Generic CMS pages now use an optional image-led content layout; `/timetable` was restored from the archived content. No homepage route or homepage content was changed.
- **Blog images fixed end to end** — Directus thumbnail values now support either UUIDs or expanded file objects; thumbnails render on blog cards, article pages, Open Graph metadata and BlogPosting JSON-LD. `npm run media` assigns archived starter images only when a post has no thumbnail. Verified both live thumbnail assets return HTTP 200 with `image/jpeg`.
- **Ticket export correction** — the browser export uses the high-resolution `toPng` data URL directly for PNG download and jsPDF input (instead of passing an unsupported Blob shape), preventing the `getImageFileTypeByImageData` undefined-data failure reported on both download buttons.
- **Applied and verified locally** — `npm run seed`, `npm run media`, fresh `npx tsc --noEmit --incremental false`, `npm run lint`, Directus record/asset checks, and HTTP 200 smoke tests for fleet, vehicle, blog list/detail, About and Timetable all passed.

- **Ticket right/bottom cropping fixed** — PNG/PDF export no longer captures the live `mx-auto` pass directly. It clones the complete measured ticket into a fixed-width export surface with 32px padding, waits for fonts and QR imagery, then rasterises that surface at 3× resolution. Auto margins, shadows, rounded corners and the QR/reference stub now remain inside the bitmap; PDF output uses the same complete image scaled onto A4.

## 2026-07-30

- **Homepage navigation and FAQ refresh** — separated the school-transport and nationwide-coverage highlights into two full-width sections, retaining a two-column presentation inside each. Nationwide coverage now gives the map and area list their own visual column. Reworked the CMS navigation into Services, School Transport, Daily Express and Company mega-menu groups, merging Contact into Company and exposing the Daily Express routes directly. Added a dedicated `/faqs` route with canonical metadata and the existing CMS-managed FAQ accordion (including FAQPage JSON-LD). TypeScript and lint checks passed.

- **Light-blue visual system + expanded FAQs** — restored Contact Us as the fifth top-level menu item. Added 13 useful FAQ entries from the archived source to the CMS seed/fallback; the homepage intentionally shows the first four while /faqs presents all entries. Reworked shared page heroes and text-led CMS pages with white and light-blue surfaces, and converted both fleet showcase areas from large dark bands. The nationwide coverage section now includes a clear CMS-driven Iver depot marker; the footer remains the primary dark-blue closing surface.

- **ULEZ blog article refresh** — rebuilt the blog-post detail layout with the shared light-blue PageHero, editorial card, responsive feature image and a compact sticky company/quote panel so it now belongs to the wider site system. Expanded the CMS seed and local Directus entry for /blog/what-ulez-compliant-means with clear ULEZ/Euro-6 guidance, planning advice and reader FAQs; the same content is available on a fresh CMS seed.

## 2026-08-06

- **Added a Coolify-native production stack** — new root `docker-compose.coolify.yml` deploys the standalone Next.js image, Directus, MariaDB and password-protected Redis without host port mappings or a competing Traefik container. Only `web:3000` and `directus:8055` are intended for Coolify domains; database/cache/migration/bootstrap services remain private. Added health checks, startup ordering, named volumes, `no-new-privileges`, Redis-backed Directus caching/rate limiting, secure proxy/cookie settings, and exact image pins (Node 22.23.1, Directus 12.0.2 matching the schema snapshot, MariaDB 11.8.8, Redis 7.4.10).
- **Automated first-deploy CMS provisioning** — added `Dockerfile.bootstrap` and its narrow build context. Coolify now applies `directus/schema-snapshot.yaml`, then runs the existing idempotent seed, admin configuration and committed ~103 MB media import before starting the web container. Bootstrap scripts accept a separate static `DIRECTUS_ADMIN_TOKEN`, retaining email/password login for local development; the admin token is never passed to the web app.
- **Documented the full Coolify launch path** — added `.env.coolify.example` and `DEPLOY_COOLIFY.md` covering repository/build-pack settings, required versus runtime-only variables, domain-to-port routing, Cloudflare, scoped Directus server permissions, Stripe/Turnstile/M365 launch checks, persistence and off-site backups. Updated README, `.env.example`, the standalone deployment runbook, and the production Dockerfile health check. No database dump was committed because production dumps can contain credentials and UK customer data; the schema/content/media sources remain reproducible and safe for Git.
- **Fixed Coolify's first-deploy schema mount** — Coolify materialised the Compose `configs.file` source as an `/artifacts/.../directus/schema-snapshot.yaml` bind mount, but that path was absent from the deployment artifact, so container creation failed. `schema-migrate` now builds `Dockerfile.schema`, which copies the committed snapshot into its image through a narrow Dockerfile-specific context; no runtime bind mount or host path is required.
- **Fixed Coolify secret generation compatibility** — the deployed Coolify release populated single-word magic identifiers (`DATABASE`, `REDIS`) but left compound identifiers such as `DATABASE_ROOT` and `DIRECTUS_KEY` empty, causing MariaDB to abort without `MARIADB_ROOT_PASSWORD`. All infrastructure secrets now use fresh single-word identifiers (`DBUSER`, `DBROOT`, `REDISPASS`, `DIRECTUSKEY`, `DIRECTUSSECRET`, `DIRECTUSADMIN`, `WEBAUTH`). This both restores automatic generation and rotates the database/Redis values exposed while diagnosing the failed deployment; `DIRECTUS_SERVER_TOKEN` remains optional on the first deploy.
- **Made the first media bootstrap resilient to Directus rate limiting** — the committed media archive can exceed Directus's default 50-request-per-second API allowance even though files are processed sequentially. `upload-media.mjs` now spaces API calls and retries `429` responses up to eight times using the server's advertised delay (with bounded exponential fallback). Existing title-based matching remains unchanged, so redeploys resume without duplicating files or overwriting editor-managed media.
- **Completed the production documentation and ownership handover** — recorded the successful Coolify deployment; added `HANDOVER.md` for service topology, repository-backed releases, secret ownership, smoke checks, backups, rollback, CMS operations and production acceptance; updated README, project facts, Coolify/standalone runbooks, maintainer guides, active plan/task board and development handoff to remove pre-implementation/awaiting-VPS/public-write/Traefik-primary/placeholder-branding statements. Standardised the repository as [`nityam2007/np-coach`](https://github.com/nityam2007/np-coach) and delivery credits as Blustdio, Product Manager [Rohan](https://rohanxblu.in/), and Developer [Nityam](https://nsheth.in/). Historical SOW, archived WordPress pages, legal inputs, brand source, and SEO archive remain unchanged.
- **Completed Turnstile server verification and Coolify auto-deploy handover** — every existing protected form now uses canonical Cloudflare Siteverify validation with an explicit action, response checks, an 8-second timeout, production fail-closed behavior, and client widget reset after each server-action result. Standardised the private runtime variable as `TURNSTILE_SECRET` while accepting `TURNSTILE_SECRET_KEY` for compatibility. Updated environment/Compose files and documented native GitHub webhook deployment from `nityam2007/np-coach` pushes to `main`; no webhook or Turnstile secret is committed.
- **Fixed the production Turnstile reset crash** — replaced the unscoped implicit-widget reset with an explicitly rendered, ID-tracked widget. Reset and unmount cleanup are guarded so Cloudflare cannot throw into the application error boundary during navigation or a server-action rerender. The CSP now permits only Cloudflare's documented Web Analytics script and beacon origins, removing the separate Browser Insights violation shown in DevTools.
- **Fixed Coolify Turnstile secret fallback** — Compose intentionally supplies an empty preferred `TURNSTILE_SECRET` when only the compatibility `TURNSTILE_SECRET_KEY` is configured. Secret selection now treats that empty value as absent, allowing the populated compatibility variable to reach Siteverify instead of failing every production spam check before the Directus write.
- **Made the complete Directus bootstrap rate-limit resilient** — seed, admin configuration, and media import now use one shared client that paces every API request, honours `Retry-After`, and applies bounded backoff for `429` and temporary upstream failures. Admin setup now fetches the collection catalogue once instead of once per group. Coolify explicitly retains Directus's Redis-backed 50-request/second limiter while the bootstrap defaults to 20 requests/second, preventing transient deployment bursts from failing the whole health-gated stack without weakening production API protection.
- **Implemented internal Postfix transactional email end to end** — SMTP transport now supports the private no-auth/no-TLS relay with an NP Coaches display name, connection pooling and safe failure logging. Added reusable branded HTML/plain-text templates with CMS-editable copy for OTP, booking tickets, lost property, contact and quote messages. OTP requests now fail visibly if production delivery fails; persisted contact/quote submissions send staff notifications and customer acknowledgements; verified booking payments send a confirmation exactly once; verified lost-property payments send separately tracked customer and staff emails. Directus schema/seed now stores per-channel delivery state/timestamps and email copy, with conditional claims preventing webhook/success-page races from intentionally duplicating messages. Updated both Compose stacks, environment inventories and production handover/runbooks for the Postfix network, `demo.np-coaches.co.uk` Stripe webhook acceptance, and mailbox smoke tests.
- **Hardened Git/Coolify deploys against production data and CMS resets** — routine releases no longer run Directus's exact `schema apply --yes`; they run only Directus system bootstrap followed by additive setup. The seed now writes the settings singleton only on first installation, preserves populated CMS values while merging only new nested keys, checks customer/booking/pass/contact/quote counts before and after bootstrap, and fails on any decrease. Directus dashboard panels are preserved instead of deleted/recreated. Manual schema apply always prints a dry-run and requires explicit verified-backup confirmation. MariaDB remains on the unchanged `mariadb_data` named volume.
- **Fixed customer account actions and redirects** — sign out now uses a dedicated full-page POST endpoint that explicitly expires the host-only session cookie and returns a 303 to the login page with confirmation. OTP resend now includes a fresh Turnstile token and displays its result; “Different email” performs a clean reload so stale action state cannot trap the user. Existing internal recipients remain unchanged: contact/lost property → `info@np-coaches.co.uk`, quotes → `bookings@np-coaches.co.uk`.

## 2026-08-10

- **Added the client image requirements specification** — created `IMAGE_REQUIREMENTS.md` from a code-level audit of every Directus media field and rendered image container. It defines the core/optional asset counts, client-facing filenames, exact display ratios, recommended source dimensions, crop-safe composition rules, file formats, fleet gallery and seating-plan sets, and replacement priorities for low-resolution or mismatched current media. Linked the document from the README; no application code or CMS data changed.

## 2026-08-14

- **Enabled Stripe-hosted promotion codes for production payments** — Daily Express and Lost Property Checkout sessions now display Stripe's customer-entered promotion-code field without hardcoding any coupon or test-mode ID. Signed webhook and success-page verification record Stripe's final discounted `amount_total` before tickets, accounts and transactional emails use it, and fully discounted no-payment Checkout sessions complete through the same idempotent flow.

## 2026-08-20
- **Completed the fleet, coverage and Redis follow-up** — removed the eight homepage coverage chips while keeping the UK depot marker; changed the interactive globe to a white/ice palette; rebuilt the fleet introduction and individual coach-detail presentation with CMS-managed highlights and labels; retained the existing no-image `PageHero` fallback; and kept per-vehicle copy, main images, galleries and seating plans editable in Directus.
- **Expanded production cache use without stale CMS content** — added automatic Directus AVIF/WebP negotiation and no-upscale transforms, raised Redis to a 768 MB LFU budget, extended Directus content/schema caching with automatic write purging, and moved contact, quote, booking, pass and OTP limits to atomic shared Redis counters with a local outage/development fail-safe.
- **Cleared production dependency advisories found during verification** — updated Next.js and its ESLint config to 16.3.1 and pinned DOMPurify 3.4.14 for the existing ticket PDF export; `npm audit --omit=dev` now reports zero production vulnerabilities.
- **Fixed intermittent production image loading at its source** — reconciled live Directus assignments and confirmed the London/Leicester, Wolverhampton and Daily Express UUIDs hash-match the committed client banners. A controlled live crawl showed automatic 3840px AVIF transforms taking 10–27 seconds, with one request exceeding 30 seconds; there were no 429 responses. The loader now emits WebP at quality 78, caps output at 2560px, removes Next's 3840px candidate, and constrains Directus to two concurrent transforms with a 15-second ceiling for the 2-vCPU VPS. Intentional no-image pages such as Get a Quote retain the striped fallback.

- **Implemented the August client review and approved Drive media pack** — imported 23 approved assets (homepage video, service/route banners, school imagery, lost-property artwork, seven tour card/hero pairs, and the current 2025–2026 compliance PDF). The media bootstrap uploads them into a dedicated Directus folder and applies revision `client-review-2026-08-20` once; later CMS edits are preserved.
- **Completed the six-slide UI review** — replaced the homepage map illustration with a draggable tactical globe carrying one UK depot marker; removed the school block's lined background; added reduced-motion-aware count-up metrics; fixed accreditation badge sizing; restored the optimized legacy homepage video; standardized image-led banners at 21:9; added a working compliance download; gave tour amenity pills icons and destination pages alternating photo/copy sections; rebuilt the fleet listing; anchored and unified From/To listboxes with opposite-stop filtering; rounded the footer and added the verified Facebook/Instagram links.
- **Extended the CMS/fallback model without direct database access** — added editable social links, tour-page copy, page attachments, image alternative text, tour hero/card images, route/service banners, and homepage video fields. Approved alternative text is seeded alongside media; shared renderers consume Directus values with `site-content.json` fallback.
- **Verified for deployment** — `node --check scripts/seed-directus.mjs`, `node --check scripts/upload-media.mjs`, `npx tsc --noEmit --incremental false`, `npm run lint`, and `git diff --check` pass. A local build/dev server was intentionally not run under the repository's process-ownership rules; Coolify's additive bootstrap will apply the revision after push.
- **Corrected client banner placement across the site** — CMS page, Daily Express, route, home-to-school, school, fleet, tour, blog, FAQ, about and lost-property media now render as full hero backgrounds with layered navy contrast overlays. Removed the separate rounded PageBanner card and duplicate about/blog/lost-property image cards.
- **Fixed homepage visual regressions** — removed every parent clipping boundary around the Daily Express selector and raised its active listbox stack so dropdowns overlay later sections; accreditation badges now render in full colour by default.
- **Made Directus edits practical and discoverable** — content ISR now refreshes in about 5 seconds; Directus Studio receives clear widths/notes for global copy/media plus services, pages, fleet, tours, routes and blog fields; existing service image descriptions are safely backfilled only when missing.
- **Prepared client media revision v2** — the next additive bootstrap reapplies the approved pack under `client-review-2026-08-20-v2` and assigns the supplied London tour card to the homepage UK Tours service. Existing editor changes remain protected after that one revision.
- **Verified the reported deployment was not a failed seed** — the demo and CMS endpoints returned 200, the configured client revision and media IDs were present, and the optimized Directus MP4 was served as video with byte-range support. TypeScript, ESLint, bootstrap-script syntax checks and diff whitespace checks pass for the correction.
- **Fixed intermittent production image loading at its source** — reconciled live Directus assignments and confirmed the London/Leicester, Wolverhampton and Daily Express UUIDs hash-match the committed client banners. A controlled live crawl showed automatic 3840px AVIF transforms taking 10–27 seconds, with one request exceeding 30 seconds; there were no 429 responses. The loader now emits WebP at quality 78, caps output at 2560px, removes Next's 3840px candidate, and constrains Directus to two concurrent transforms with a 15-second ceiling for the 2-vCPU VPS. Intentional no-image pages such as Get a Quote retain the striped fallback.
- **Corrected the post-deploy Directus transform capacity** — a six-way production burst test returned 503 for two of 40 assets because the initial two-transform ceiling rejects excess work instead of queueing it. Raised the ceiling to eight now that capped WebP transforms complete in roughly 0.5–1.5 seconds; the 15-second guard remains, and the same test must complete with zero 429/503 responses before handoff.
- **Made every missing-image hero CMS-backed and fixed disappearing hero copy** — route, tour, school and fleet foregrounds now occupy an explicit layer above images and overlays, preventing decoded backgrounds from painting over their text. Added three global Directus file selectors with editable alt text, safely populated from the existing Wolverhampton, London–Leicester and Daily Express banners only when empty. Shared PageHero consumers, Daily Express booking and Lost Property claim now reuse those UUIDs whenever their own CMS image is absent; page-specific media remains authoritative.
- **Stopped production builds from baking image-less offline pages** — server CMS reads now fall through from the private runtime `DIRECTUS_URL` to the public `NEXT_PUBLIC_DIRECTUS_URL` used by the Docker builder. SSG therefore reads the healthy existing Directus deployment during Coolify builds, while the running container still uses the private network; first visitors no longer need to trigger a five-second ISR repair to see CMS media.

## 2026-08-20 — production-readiness hardening

- Sanitized every CMS HTML surface, restricted CMS-authored URLs to safe schemes, and escaped all embedded JSON-LD against script termination.
- Reworked Directus reads around bounded timeouts, explicit outage fallback, five-minute ISR/cache tags, and a protected on-demand revalidation endpoint; empty or deleted CMS records no longer resurrect fallback content.
- Hardened OTP and abuse controls with keyed HMACs, atomic single-use consumption, attempt caps, bounded local memory, trusted Cloudflare IP handling, and fail-closed production Redis behaviour.
- Hardened Stripe state handling with cryptographic unique references, safe session attachment/failure paths, unique reconciliation, idempotent paid updates, and failed/expired asynchronous event handling.
- Corrected Daily Express modelling for stable stop codes, both directions, Leicester service days, route-specific fares, real dates/returns/departures, cutoff validation, terms consent, and the missing Leicester stop and London–Wolverhampton timetable.
- Replaced passenger-bearing QR payloads with signed verification URLs whose API checks the live paid booking without exposing personal data.
- Isolated MariaDB and Redis networks, disabled unused Directus transports, changed Redis to `noeviction`, narrowed public file metadata, upgraded Directus, and added required security secrets to the deployment template.
- Made backups encrypted and off-site by default for both MariaDB and uploads, and added a disposable restore test; added CI tests, type/lint/build checks, dependency auditing, and cleared all npm advisories.
- Added verified company disclosure, legacy redirects, canonical home metadata, a security contact file, a keyboard-native route selector, skip navigation, secure consent persistence, and persistent cookie settings.
- Production Daily Express checkout now defaults to fail-closed until atomic per-departure capacity is implemented and accepted. External Cloudflare, Stripe, M365, backup, legal/ICO, Directus-role, accessibility, and launch checks remain operator-owned and are tracked in `SECURITY_AND_OPERATIONS.md`.


## 2026-08-22 — production-readiness remediation expansion

- Added a required private Directus endpoint protected by the scoped Directus user and an independent timing-safe shared secret. It performs SQL-level compare-and-swap updates and row-locked inventory transactions while preserving the rule that only Directus connects to MariaDB.
- Implemented atomic one- and two-leg Daily Express inventory with unique `service_runs`, CMS-approved per-route capacity, last-seat enforcement, pending holds, paid commits, and safe failed/expired/stale release. Checkout remains disabled by default until the extension/schema are deployed and live concurrency acceptance passes.
- Replaced concurrency-sensitive filtered REST updates with database-atomic OTP consumption, Checkout attachment, paid/failed/refunded/disputed transitions, inventory-release claims, and lease-token email delivery state.
- Added opaque hashed revocable customer sessions, an authenticated hourly maintenance endpoint, bounded contact/quote delivery retries, stale order cleanup, and persisted delivery visibility.
- Reconciled anonymous Directus permissions, added published/draft workflow enforcement, restricted public file metadata, and made existing payment status dropdowns include refunded/disputed states.
- Completed additional keyboard/focus/form-error controls, persistent carousel/video pause controls, reduced-motion and Save-Data handling, CMS-driven Open Graph imagery, richer Organization/BlogPosting data, and accurate Service schema for tours and scheduled routes.
- Mounted the required extension read-only in Coolify, local development, and the standalone deployment alternative; pinned Directus 12.1.0 and documented the six independent audit secrets and hourly maintenance schedule. Transactional email remains the internal Postfix relay with real Microsoft 365 mailbox delivery as production acceptance.
- Expanded automated coverage to six tests, including authenticated compare-and-swap and transactional two-leg reservation/release behavior. Unit tests, script syntax, TypeScript, ESLint, all 54 production routes, diff checks, Compose YAML parsing, and `npm audit --omit=dev --audit-level=high` pass; Docker-backed extension startup and restore testing remain operator checks because Docker is unavailable on this workstation.

## 2026-08-22 — Directus 12.1 deployment hotfix

- Added the required `source` field to the internal endpoint manifest and verified Directus 12.1 loads both repository extensions.
- Replaced the Directus 12.1-incompatible filtered bulk item update with idempotent per-row status repairs; the exact production `services.status IS NULL` failure now completes successfully.
- Added an API hook that forces `status=published` on every anonymous content query/read, plus matching filters on all website CMS reads, without relying on edition-gated custom permission rules.
- Switched public content permission reconciliation to Directus's standard permission shape, preserved the existing public-file permission unchanged during upgrades, and stripped anonymous file API responses to display-safe metadata without breaking transformed asset delivery.
- Verified the complete seed/configure/media bootstrap against a backed-up MariaDB volume on Directus 12.1, including a second idempotent run and direct anonymous draft/null leak tests.

## 2026-08-22 — Analytics, apex SEO and Directus Studio repair

- Reproduced the CMS black screen in headless Chrome and identified the actual Directus 12.1 Studio crash (`init_runtime_dom_esm_bundler is not defined`), rather than treating successful API/asset responses as proof that the SPA mounted.
- Pulled Directus 12.3.0, confirmed its rebuilt Studio bundle no longer contains the undefined call, and booted a disposable SQLite-backed container whose rendered title reached `Sign In · Directus` without a console exception. Pinned 12.3.0 across Coolify, local, standalone and schema-bootstrap images.
- Started the real local Compose stack on 12.3.0: the Directus system migration completed, both NP extensions loaded, and `/server/ping` returned `pong`. Existing MariaDB and uploads mounts were retained; the pre-existing backup volume was not modified.
- Added consent-gated GA4 measurement `G-3GXSRDBP55` using basic consent mode: no Google tag before acceptance, advertising consent/signals disabled, manual path-only page views, and first-party GA cookie cleanup after rejection.
- Preserved the existing apex Search Console HTML verification token through public build variables and expanded the production CSP only for the required Google tag/Analytics endpoints.
- Expanded site-wide LocalBusiness JSON-LD with verified company, address, contact, area, currency and CMS service-catalog data; existing sitemap, robots, canonicals and `llms.txt` remain rooted at `https://np-coaches.co.uk`.
- Made cookie-banner wording editable in Directus and added a revision-guarded, one-time update to the existing Privacy and Cookie Policy records covering GA4, Search Console, Google Maps, consent withdrawal, cookie durations and international processing. Future CMS edits are preserved.

## 2026-08-22 — Directus 12.3 schema launcher hotfix

- Fixed the failed 12.3 deployment where `schema-migrate` exited with `/bin/sh: npx: not found`; Directus 12.3's slim runtime image no longer includes npm/npx.
- Replaced every container-side `npx directus` invocation with the image's verified `node /directus/cli.js` entrypoint, including the guarded manual snapshot/apply helper.
- Ran the exact replacement `node /directus/cli.js bootstrap` command against a disposable SQLite database in the pinned 12.3.0 image; all system migrations completed and the process exited successfully.

## 2026-08-22 — CMS consent seed ordering hotfix

- Moved the cookie-consent settings merge before its change detector after production exposed a temporal-dead-zone error during `cms-bootstrap`.
- Confirmed the failed run had only added the two nullable settings fields before stopping; no customer, booking, payment, or uploaded-media records were altered.
- Added a source-order verification alongside the existing seed syntax, script, and test checks before redeployment.

## 2026-08-22 — CMS media, inventory guidance and booking-state repair

- Repaired all 18 Directus image/video fields by idempotently creating their missing relations to `directus_files`; the fields previously rendered a File/Image interface without a real File Library relationship.
- Kept `service_runs` visible and documented it as the automatically-created route/date/departure inventory ledger, with clearer field metadata and route-capacity guidance.
- Decoupled fare display from sellable capacity so configured single/return prices remain visible while checkout accurately distinguishes a closed launch flag, zero capacity, an unsupported direction, and insufficient seats.
- Hardened the hero video control with an explicit muted retry and poster fallback on media failure; renamed the homepage services motion control from “services” to “carousel”.
- Ran the complete additive seed and Directus admin configuration against the local MariaDB-backed 12.3.0 stack; all 18 File Library relations were present, 15 existing dashboard panels were preserved, and protected record counts did not decrease.

## 2026-08-22 — Daily Express multi-service discovery and implementation brief

- Audited the public WordPress homepage, Daily Express hub, four published bus records, two direction taxonomies, Leicester route pages, acceptance deployment, public production Directus route/stop data, and the local booking/payment/inventory path.
- Confirmed the required hierarchy: two online route directions with two 60-seat daily services each, while London ↔ Leicester remains Friday–Monday and driver-sale only.
- Documented the current first-overlapping-route fare bug, missing departure choice, flat-fare/return limitations, timetable conflicts, incomplete ticket/email leg details, and the safe disabled-capacity state.
- Added `AIDATA/IMP-22-8-26.md` as the authoritative implementation brief with provisional schedules, confirmed fare evidence, missing approval inputs, additive Directus model, customer flow, code map, tests, rollout/rollback gates, and a copy-paste implementation prompt. Original user raw notes were preserved.
- Updated the active plan, task board, and continuation handoff. No application code, production CMS data, or booking flag changed.

## 2026-08-22 — Daily Express multi-service implementation

- Added the additive `scheduled_services` Directus/fallback model with stable service codes, ordered boarding/dropping stops, explicit ISO operating days, per-stop-pair fare rows, sales mode and capacity; retained all populated legacy `routes` fields and URLs.
- Seeded the six researched schedules additively. The four London–Midlands services are online-capable and 60-seat; both Friday–Monday Leicester services are `driver_only`. Only documented fares are present, and every missing fare fails closed pending operator approval.
- Replaced first-overlapping-route resolution with deterministic all-service search and explicit outward/return selection. The server re-resolves service code, stop order, date, fare, capacity and one-hour Europe/London cutoff before persisting or contacting Stripe.
- Keyed atomic dated inventory by `service_code:YYYY-MM-DD` while retaining route/time snapshots and conservative whole-service capacity. Two-leg reservation/release remains row-locked and transactional through Directus.
- Persisted immutable outward/return journey and fare snapshots on bookings; added service names, departure/arrival times and both legs to confirmation email, account summaries, boarding passes and signed QR validation claims.
- Excluded driver-only Leicester from homepage/book selectors, inventory, Stripe and online Offer schema while keeping its timetable/SEO pages and driver-sale notice. Route pages and the Daily Express hub now show every child service.
- Expanded deterministic resolver and Directus extension coverage; 17 tests, TypeScript, ESLint, seed/configure syntax checks and `git diff --check` pass.
- Kept `DAILY_EXPRESS_BOOKINGS_ENABLED=false`. No production CMS data, Stripe configuration, schema snapshot or launch flag was changed; the complete fare matrix, timetable approval, passenger/return rules and live acceptance gate remain required.
