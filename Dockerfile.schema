# Packages the committed Directus schema into the one-shot migration image.
# This avoids Coolify translating a Compose file config into an unavailable
# /artifacts bind mount during Git deployments.
FROM directus/directus:12.0.2

COPY --chown=node:node directus/schema-snapshot.yaml /directus/schema-snapshot.yaml
